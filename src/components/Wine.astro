---
interface Props {
	winery: string;
	wine: number;
	rating: {
		average: number;
		reviews: string;
	}
	location: string;
	image: string;
}

const { winery, wine, rating, location, image } = Astro.props;
---

<div class="wine">
    <div class="wine-image">
        {image && 
            <img src={image} alt={wine + " bottle."}>
        }
    </div>
    <div class="wine-details">
        <h1>{wine}</h1>
        {winery && 
            <p>Winery: {winery}</p>
        }
        {location &&
            <p>Location: {location}</p>
        }
        {rating.average &&
            <p>Average rating: {rating.average}</p>
        }
    </div>
</div>